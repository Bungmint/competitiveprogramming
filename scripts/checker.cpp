#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;
using pii = pair<int, int>;
using ll = long long;
#define pb push_back
#define fi first
#define se second
#define ar array
#define sz(x) (int)(x).size()

int main(int argc, char *argv[])
{
    ifstream fin("input_file", ifstream::in);
    ifstream ans("myAnswer", ifstream::in);
    ifstream cor("correctAnswer", ifstream::in);
    ofstream fout("checker_log", ifstream::out);
    return 0;
}